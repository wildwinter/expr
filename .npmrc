# @wildwinter packages (expr, scoperegistry) publish to and resolve from the
# public npm registry (the default), so no scope-registry mapping is needed.
# Within this monorepo the two packages resolve to each other as workspaces.
